name: "Trigger Android Cross-Compilation Test"

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # 1. Enable RDP & disable Network Level Authentication (NLA)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 2 -Force

          # 2. Open Firewall port 3389 cleanly for incoming remote streams
          Remove-NetFirewallRule -DisplayName "RDP-Tailscale" -ErrorAction SilentlyContinue
          New-NetFirewallRule -DisplayName "RDP-Tailscale" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 3389

          # 3. Apply configurations via structural restarts
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          if (Get-LocalUser -Name "kaliuser" -ErrorAction SilentlyContinue) {
              Remove-LocalUser -Name "kaliuser"
          }
          $securePass = ConvertTo-SecureString "KaliPass2026!" -AsPlainText -Force
          New-LocalUser -Name "kaliuser" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "kaliuser"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "kaliuser"
          Write-Host "Static credentials for user 'kaliuser' registered successfully."

      - name: Install System Dependencies (Tailscale, Git, & Docker Engine)
        run: |
          Write-Host "Deploying tooling dependencies via Choco as Admin..."
          # Clean installation profile containing only essential container runtimes
          choco install tailscale git docker-cli docker-compose -y --no-progress
          
          $exePath = "C:\Program Files\Tailscale\tailscale.exe"
          $timeoutSec = 45
          $elapsed = 0
          while (-not (Test-Path $exePath) -and $elapsed -lt $timeoutSec) {
              Start-Sleep -Seconds 2
              $elapsed += 2
              Write-Host "Verifying service registration profile on system... ($elapsed s)"
          }

      - name: Establish Tailscale Connection
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $env:Path += ";C:\Program Files\Tailscale"
          $exePath = "C:\Program Files\Tailscale\tailscale.exe"

          if (-not (Test-Path $exePath)) {
              Write-Error "CRITICAL: Tailscale installation step did not finish routing files cleanly."
              exit 1
          }

          Write-Host "Connecting tunnel node..."
          & "$exePath" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=lab-tablet-runner --reset
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 12) {
              $tsIP = & "$exePath" ip -4
              if ($tsIP) { $tsIP = $tsIP.Trim() }
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale overlay engine timed out."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Deploy, Configure, and Launch RedAmon Automatically
        run: |
          Write-Host "Cloning RedAmon repository to D:/ drive..."
          cd D:/
          
          # Clean clone using exact repository target path to avoid string merging errors
          git clone https://github.com
          
          cd D:/redamon
          
          if (Test-Path ".env.example") {
              Write-Host "Configuring Environment Variables..."
              $envContent = Get-Content .env.example
              
              # Automatically flip the flag to turn on internal Tor routing natively
              $envContent = $envContent -replace 'USE_TOR_FOR_RECON=false', 'USE_TOR_FOR_RECON=true'
              $envContent | Set-Content .env
              Write-Host "Tor successfully activated within RedAmon configuration profile."
              
              # Automated boot sequence execution engine
              Write-Host "Launching background containers..."
              docker-compose up -d
              Write-Host "RedAmon multi-container infrastructure deployed successfully."
          } else {
              Write-Warning "Configuration templates missing. Multi-container initialization skipped."
          }

      - name: Verify RDP Accessibility
        run: |
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "Local interface pipeline is actively dropping port 3389 packets."
              exit 1
          }

      - name: Maintain Active Session & Await Tablet Connection
        run: |
          Write-Host "`n==============================================="
          Write-Host "🚀 STREAMLINED DOCKER WORKSPACE LIVE AND READY"
          Write-Host "==============================================="
          Write-Host "Tailscale IP   : $env:TAILSCALE_IP"
          Write-Host "Username       : kaliuser"
          Write-Host "Password       : KaliPass2026!"
          Write-Host "RedAmon Access : http://$env:TAILSCALE_IP:3000"
          Write-Host "Config Mode    : Direct OS Container Execution (No Hyper-V)"
          Write-Host "===============================================\n"
          
          # Keep session alive for tablet interaction
          $elapsed = 0
          while ($elapsed -lt 18000) {
              Write-Host "[$(Get-Date)] Monitoring node health. Continue working from your Samsung Tab..."
              Start-Sleep -Seconds 300
              $elapsed += 300
          }

